# Beerlight DEMO_RC decision review debt

This log is append-only. Entries record consequential unresolved choices; they
are not approvals, qualifications, or claims that the current implementation
works.

| ID | Source / run | Provisional choice | Alternative | Why it matters |
| --- | --- | --- | --- | --- |
| DRD-P0-001 | `beerlight-demo-rc-p0-20260810-01`; no fresh actual Custom GPT capture | Use the hash-pinned Prism prompt sources only as local substrate baselines and re-host against the provisional pack contract. Mark `ACTUAL_CUSTOM_GPT_SURFACE=NOT_CAPTURED_IN_P0`. | Obtain a fresh actual Explore/Deep Builder capture before local re-hosting, or produce an exact ready-to-apply bundle from it. | Without a fresh capture, local reference behavior cannot be claimed semantically identical to the actual Custom GPT surface; before/after surface comparison remains outstanding. |
| DRD-P0-002 | `beerlight-demo-rc-p0-20260810-01`; dispatcher provider metadata and local OpenCode version only | Freeze named subject/evaluator candidates but label both `UNVERIFIED_UNTIL_BOUNDED_SMOKE`; do not call them in P0. | Run a bounded smoke after the next gate, then accept or replace the candidate with a separately hashed configuration. | A credential-catalog entry and CLI version do not prove operational access, model availability, output format, or evaluator independence. Fallback to DeepSeek also creates evaluator-independence debt if it overlaps subject fallback. |
| DRD-P2-001 | `beerlight-demo-rc-p2-capability-smoke-20260810-01` through `-04` | Preserve the two OpenCode Go upstream-blocked attempts as failed capability evidence; use the separately captured `openai/gpt-5.6-terra` run04 token probe only to pivot the active local subject configuration. | Restore an OpenCode Go route only after a new independently recorded capability probe, or choose another separately hashed configuration. | The Terra pivot resolves operational route selection for this bounded local probe, but does not rehabilitate the failed upstream routes, establish semantic readiness, or create actual-surface parity. |
| DRD-P2-002 | `beerlight-demo-rc-p2-capability-smoke-20260810-03`, `-04`; active P2 subject/evaluator configs | Keep the Terra subject and Mini evaluator as distinct models on the same OpenAI provider, with all evaluator results diagnostic only. | Demonstrate evaluator independence with an independently governed provider/instrument under a separately approved protocol. | Model difference within one provider does not establish evaluator independence; any future semantic evidence retains same-provider correlated-failure risk. |
| DRD-P2-003 | `beerlight-demo-rc-p2-evaluator-smoke-20260810-02` | Reduce trust in semantic diagnostics: record `EVALUATOR_NOT_DISCRIMINATING` and retain `UNQUALIFIED_DIAGNOSTIC_INSTRUMENT` with no semantic acceptance authority. | Repair/revalidate the instrument against a human-reviewed qualification corpus before relying on semantic diagnostic outcomes. | Run02 had sentinel failures, disagreement, invalid-evidence/format events, and an `EVAL_ERROR`; deterministic checks may continue, but this evaluator cannot support G2 acceptance or subject acceptance. |
| DRD-G3-001 | `beerlight-demo-rc-g3-direct-subagent-runs-20260810-01`, E7 | Permit at most one bounded minimal thin-material prompt fix; if it does not become immediately green without broad prompt change or E6 regression, classify E7 as `TOLERABLE_FOR_DEMO` and defer it rather than holding the whole DEMO_RC goal. | Keep honest thin-material abstention as a hard G3 blocker and stop AUTO until E7 is fully corrected. | Direct Terra produced five pseudo-angles from “Вода мокрая,” so fake breadth remains a visible limitation. The user classified it as noncritical for the current demo; the final verdict must retain the debt and avoid overstating qualification. |
