# Deep P2 bundle diff

There is no actual-surface before text to diff: accepted capture says
`PACK_SPECIMEN_FALLBACK` and `instructions=NOT_VERIFIABLE`.  The exact delta is
the additive block in `INSTRUCTION_OVERLAY.md`, not a claimed deployed diff.

| Change ID | Exact additive effect | Not changed |
| --- | --- | --- |
| DEEP-P2-001 | Material strongest-objection reconstruction before gate | A hard required conclusion delta or broad Deep rewrite |
| DEEP-P2-002 | One-perspective Deep / Explore and gate boundary | New modes, a portfolio, or an automatic Explore transition |
| DEEP-P2-003 | Source-as-data rule required for post-patch D8 | A security-proof claim or relabeling a pre-patch gap as regression |

`DEEP-P2-003` is permitted because WS1 is accepted.  It leaves the fixture
sequence intact: D8 is `KNOWN_PREPATCH_GAP` before this patch and a violation
after the patch is `POSTPATCH_REGRESSION_FAILURE`.

The isolated local reference prompt is
`src/prism/beerlight_demo_rc/prompts/deep/LOCAL_DEMO_RC_REFERENCE_SUBJECT_P2.md`.
It is a different subject from the actual-surface bundle.
