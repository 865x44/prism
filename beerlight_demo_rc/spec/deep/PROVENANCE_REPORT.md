# Deep D1-D8 reconciliation provenance

Status: `PROVISIONAL_CANDIDATE_NOT_ACCEPTED`. This is a fixture-authoring and structural-validation record, not a subject or evaluator acceptance result.

## Authority and reconciliation rule

`R2_DEEP_CURRENT_STATE.md` and `R2_DEEP_SPEC_CANDIDATE.md` supply the current Deep core. `CONTRACT_DECISIONS_PROVISIONAL.md` adds the shared source-as-data invariant. `DEEP_ACCEPTANCE_V1_PROVISIONAL.md` is the candidate body, and the sparse matrix fixes predicate assignment. The historical plan is read only for the surviving notion that there were eight Deep slots; its older D-number meanings do not control current identifiers.

| Current ID | Decision | Minimal rationale | Primary evidence |
|---|---|---|---|
| D1 | MERGE | Candidate deliberately merges historical direct-seed and P-ID-recovery concerns into selected-claim/hidden-frame preservation. | Candidate D-case selection; R2 focus lock and preservation. |
| D2 | REPLACE | Historical P-ID slot is not retained as a D2 identity; current D2 protects material development of one model. | Candidate D2; R2 strongest-honest-model and Deep-not-Explore core. |
| D3 | REWRITE | Retains adversarial reconstruction but states the required strongest-objection sequence and the evidence burden for an unchanged conclusion. | R2 adversarial pass; candidate D3; sparse matrix. |
| D4 | REPLACE | Current fixture isolates evidence-debt laundering rather than preserving an old ordinal role. | Candidate D4; R2 epistemic discipline. |
| D5 | MERGE | Candidate merges the historical NEED_EVIDENCE concern with the LEVER block, as the current gate boundary requires. | Candidate D-case selection; R2 gate and LEVER boundary. |
| D6 | MERGE | Candidate merges honest return with the no-secret-Explore stop boundary. | Candidate D-case selection; R2 RETURN_TO_EXPLORE and mode boundary. |
| D7 | MERGE | Candidate merges downstream/render fidelity and revision semantics into renderer preservation of ModelLock. | Candidate D7; R2 revision semantics. |
| D8 | REPLACE | Current D8 is the post-patch Deep source-as-data sentinel, not the historical renderer-revision ordinal. | Contract source-as-data section; candidate D8; R2 records pre-patch source boundary as partial. |

## Fixed reconciliation outcomes

- Fixture identifiers are exactly `D1` through `D8`; no ninth case, predicate, quota, lifecycle state, or ontology is introduced.
- D3 runs pre-model -> strongest objection -> materiality test -> rebuilt model -> gate. An unchanged conclusion is permitted only when the response demonstrates that the tested strongest objection is non-load-bearing; a `however` section does not satisfy this.
- D8 is `POST_PATCH_ONLY`. Before the bounded source-as-data patch, its result class is `KNOWN_PREPATCH_GAP`. After that patch, a D8 violation is `POSTPATCH_REGRESSION_FAILURE`; it is never retrospectively labeled a pre-patch regression.
- The suite remains provisional. The artifacts do not assert gold labels, evaluator qualification, subject behavior, implementation success, or acceptance.

## Unresolved ambiguities retained (not resolved here)

1. Nested-mechanism detail versus a material new model has no scalar threshold.
2. D3 may legitimately retain a conclusion after an objection only with demonstrated non-load-bearing reasoning; the supplied D3 facts make that difficult rather than impossible by fixture wording.
3. Conditional `MODEL_READY` versus `NEED_EVIDENCE` depends on whether the missing fact is decisive for the requested verdict; D4 intentionally allows both honest outcomes.
4. `RETURN_TO_EXPLORE` versus salvageable narrowing remains judgment-based; D6 uses direct source contradiction to reduce, not erase, the edge.
5. D8 cannot establish complete prompt-injection security; it tests the defined semantic authority boundary only.

## Generated artifacts

- `deep_d1_d8_fixtures.json`: canonical machine-readable cases.
- `deep_sparse_mapping.json`: sparse deterministic-first predicate mapping.
- `deep_sequence_metadata.json`: D3 and D8 sequencing/classification metadata.
- `validate_deep_suite.py`: offline structural and immutable-pack validator.
- `prism-runs/beerlight-demo-rc-p1-ws1-20260810-01/source_hashes.json`: flat SHA-256 manifest of the immutable pack at reconciliation time.
